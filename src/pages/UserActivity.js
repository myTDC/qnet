import React from 'react';
import PropTypes from 'prop-types';

const UserActivity = (props) => {
	return (
		<div>
			Hi! <br />
			I'm user UserActivity tracker
		</div>
	);
};

UserActivity.propTypes = {};

export default UserActivity;
