import React from 'react';
import PropTypes from 'prop-types';

const Profile = (props) => {
	return (
		<div>
			Hi! <br />
			I'm user Profile
		</div>
	);
};

Profile.propTypes = {};

export default Profile;
